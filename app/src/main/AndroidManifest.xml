<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ao.crud.fenixinovation.firebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrudFenixInovationFirebase">

        <activity
            android:name=".ui.pages.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CrudFenixInovationFirebase.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.pages.create.CreateNoteActivity"
            android:label="Create Notes"
            android:parentActivityName=".ui.pages.MainActivity"
            android:theme="@style/Theme.CrudFenixInovationFirebase.NoActionBar">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.pages.MainActivity" />

        </activity>

        <activity
            android:name=".ui.pages.authentication.register.RegisterAccountActivity"
            android:label="Register Account"
            android:theme="@style/Theme.CrudFenixInovationFirebase.NoActionBar" />

        <activity
            android:name=".ui.pages.authentication.login.LoginAccountActivity"
            android:label="Login Account"
            android:theme="@style/Theme.CrudFenixInovationFirebase.NoActionBar" />

        <activity
            android:name=".ui.pages.update.UpdateNoteActivity"
            android:label="Update Note"
            android:parentActivityName=".ui.pages.MainActivity"
            android:theme="@style/Theme.CrudFenixInovationFirebase.NoActionBar">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.pages.MainActivity" />

        </activity>

    </application>

</manifest>